import { PreventClickDirective } from './prevent-click.directive';

describe('PreventClickDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventClickDirective();
    expect(directive).toBeTruthy();
  });
});
